!STD
    def chord_represented(self, pose, chord):
        voiced_tones = (pose[pose != -1] + self.open_strings[pose != -1]) % 12
        return all([self.tone_dict[tone] in voiced_tones for tone in chord])

        