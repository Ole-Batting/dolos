!STD
    def expand_tree(self, positions, tree, depth, chord):
        if depth == 6:
            positions.append(chord)
        else:
            for branch in tree[depth]:
                self.expand_tree(positions, tree, depth + 1, [*chord, branch])
        if not depth:
            return np.array(positions)
            
            
